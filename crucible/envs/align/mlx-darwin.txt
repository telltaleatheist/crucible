# crucible `align` env — mlx-darwin (Qwen3-ForcedAligner on Apple Silicon, through MPS).
#
# archive-bytes: 900000000  PHASE20-CODE-NOT-ENVIRONMENTS.md section 0's "~0.9 GB mlx", measured 2026-09-18
#   THE MLX TOTAL, and the only measured mlx number there is: that table prices
#   the five cuda recipes one by one and the five mlx ones together. So every
#   mlx recipe carries the total rather than a share of it invented here — it
#   overstates one env and can never understate it, and a floor may only ever
#   be wrong in that direction (PHASE19-AUTOMATIC-WSL.md ruling 6). An ARCHIVE
#   size; the unpacked env is larger. Replace this line the day one mlx env is
#   measured on its own — one recipe, one measured number, one owner.
#
# Installed by `crucible install align` into ~/.crucible/envs/align/ as a venv
# built from the server's own interpreter. Everything here comes from PyPI.
#
# A RESOLVED SET, read from a real env that has run this aligner
# --------------------------------------------------------------
# What follows is `pip freeze` from the Mac Studio's `qwen-align` conda env
# (`/opt/homebrew/Caskroom/miniconda/base/envs/qwen-align`, python 3.11.16), read
# on 2026-09-14. That env is BookForge's — it is what
# `electron/components/qwen-align-env.ts` builds, packs and publishes as
# `qwen-align-env-macos-arm64.tar.gz` — and it is the env the 97x measurement
# below was taken in. It is not a set anybody reasoned about from the cuda file.
#
# Against `envs/align/cuda-linux.txt` the difference is exactly the accelerator
# and nothing else: the 19 `nvidia-*`/`cuda-*` wheels and `triton==3.8.0` are
# absent, `platformdirs` is one patch newer (4.11.8 against 4.11.7), and every
# other line — `qwen-asr==0.0.6`, `torch==2.14.0`, `transformers==4.57.6`
# included — is identical. `torch==2.14.0` on macOS arm64 IS the Metal build;
# there is no separate wheel to ask for and no index to point at.
#
# TWO LINES OF THAT FREEZE ARE NOT HERE, and each is named rather than dropped
# quietly:
#
#   conda-pack==0.9.2   the tool that PACKED that env into the published
#                       tarball. It is BookForge's packaging step, not something
#                       the aligner imports, and Crucible packs nothing at all:
#                       it pips this file into a venv on the machine that runs it.
#   torchaudio==2.11.0  present in BookForge's env and absent from the WORKING
#                       cuda env beside this file, which is the proof it is not
#                       needed: `qwen-asr` 0.0.6 does not declare it
#                       (transformers, nagisa, soynlp, accelerate,
#                       qwen-omni-utils, librosa, soundfile, sox, gradio, flask,
#                       pytz) and `crucible/jobs/align/worker.py` imports numpy,
#                       soundfile, torch and qwen_asr.
#
# WHY THE WEB PACKAGES ARE HERE — `envs/align/cuda-linux.txt` carries that note
# and it applies unchanged: gradio, flask, fastapi and uvicorn are `qwen-asr`'s
# own declared requirements, and this file's job is to describe an env that
# WORKS.
#
# Do not add faster-whisper or ctranslate2, for cuda-linux.txt's reason. The
# Mac's whisper is `mlx-whisper` and it has an env of its own
# (`envs/asr/mlx-darwin.txt`).


# --- the set on that machine, recorded exactly ---

accelerate==1.12.0
annotated-doc==0.0.5
annotated-types==0.8.0
anyio==4.15.1
audioread==3.1.0
av==18.1.0
blinker==1.9.0
brotli==1.2.0
certifi==2026.7.22
cffi==2.1.1
charset-normalizer==3.5.1
click==8.5.0
cloudpickle==3.1.2
Cython==3.3.0
decorator==5.3.1
dyNET38==2.2
fastapi==0.141.1
filelock==3.32.5
Flask==3.1.3
fsspec==2026.7.0
gradio==6.17.3
gradio_client==2.5.0
groovy==0.1.2
h11==0.16.0
hf-gradio==0.4.1
hf-xet==1.6.0
httpcore==1.0.9
httpx==0.28.1
huggingface_hub==0.36.2
idna==3.19
itsdangerous==2.2.0
Jinja2==3.1.6
joblib==1.6.0
lazy-loader==0.5
librosa==0.11.0
llvmlite==0.49.0
markdown-it-py==4.2.0
MarkupSafe==3.0.3
mdurl==0.1.2
mpmath==1.3.0
msgpack==1.2.2
nagisa==0.2.11
narwhals==2.26.0
networkx==3.6.1
numba==0.67.0
numpy==2.4.6
orjson==3.12.0
packaging==26.3
pandas==3.0.5
pillow==12.3.0
platformdirs==4.11.8
pooch==1.9.0
psutil==7.2.2
pycparser==3.0
pydantic==2.13.5
pydantic_core==2.46.5
pydub==0.25.1
Pygments==2.21.0
python-dateutil==2.9.0.post0
python-multipart==0.0.32
pytz==2026.3.post1
PyYAML==6.0.3
qwen-asr==0.0.6
qwen-omni-utils==0.0.9
regex==2026.9.3
requests==2.34.2
rich==15.0.0
safehttpx==0.1.7
safetensors==0.8.0
scikit-learn==1.9.0
scipy==1.17.1
semantic-version==2.10.0
shellingham==1.5.4
six==1.17.0
soundfile==0.14.0
sox==1.5.0
soxr==1.1.0
soynlp==0.0.493
starlette==1.6.0
sympy==1.14.0
threadpoolctl==3.6.0
tokenizers==0.22.2
tomlkit==0.14.0
torch==2.14.0
tqdm==4.70.0
transformers==4.57.6
typer==0.27.2
typing-inspection==0.4.4
typing_extensions==4.16.0
urllib3==2.7.0
uvicorn==0.52.4
Werkzeug==3.1.8
