# crucible `llm` env — mlx-darwin (mlx-lm on Apple Silicon).
#
# archive-bytes: 900000000  PHASE20-CODE-NOT-ENVIRONMENTS.md section 0's "~0.9 GB mlx", measured 2026-09-18
#   THE MLX TOTAL, and the only measured mlx number there is: that table prices
#   the five cuda recipes one by one and the five mlx ones together. So every
#   mlx recipe carries the total rather than a share of it invented here — it
#   overstates one env and can never understate it, and a floor may only ever
#   be wrong in that direction (PHASE19-AUTOMATIC-WSL.md ruling 6: "a floor
#   that is too low still beats a pip that dies at 4.9 GB"). An ARCHIVE size;
#   the unpacked env is larger. Replace this line the day one mlx env is
#   measured on its own — one recipe, one measured number, one owner.
#
# Installed by `crucible install llm` into ~/.crucible/envs/llm/ as a venv built
# from the server's own interpreter. Everything here comes from PyPI.
#
# `mlx-lm==0.31.3` is the pin that was CHOSEN. Everything under it is the set
# pip resolved on Owen's Mac Studio (M1 Ultra, python 3.11.16, 2026-09-12) —
# recorded exactly, because a later install must not drift. `crucible doctor`
# calls the env NOT READY if any one of these is absent or at another version.
#
# Install time on that host: 14 s.
# Regenerate after any deliberate bump with:
#     ~/.crucible/envs/llm/bin/python -m pip freeze
#
# MLX-VLM IS HERE, SINCE 2026-09-21 — `mlx-vlm==0.7.1`, THE PAGES ENGINE
# ------------------------------------------------------------------------
# `mlx-darwin`'s `pages` engine (crucible/manifests.py) is Crucible's own page
# server, `crucible/engines/mlx_vlm_serve.py`, which runs dots.ocr through
# mlx-vlm IN PROCESS out of this env — mlx-vlm's own HTTP server never puts
# the image into the prompt, which is why this line was missing for a week
# (the run is in crucible/engines/mlx_vlm.py). It is in THIS env and not a
# third one because its pins resolve under the set below unchanged.
#
# WHY 0.7.1 AND NOT 0.6.10, MEASURED ON THE MAC STUDIO 2026-09-21. 0.6.10 was
# the version the in-process path was first validated on, and it does not run
# on this env's `mlx==0.32.2`: `mlx_vlm/models/dots_ocr/vision.py:286` calls
# `mx.repeat(seq_len, grid_thw[i, 0])` with an ARRAY count, which 0.32.0
# accepted and 0.32.2 refuses (`TypeError: repeat(): incompatible function
# arguments`). 0.7.1 casts it (`int(grid_thw[i, 0])`) and keeps the batch
# surface the reader uses. So the choice was 0.7.1 on the mlx already here,
# or 0.6.10 with mlx pinned BACK to 0.32.0 under the text engine that was
# measured on 0.32.2 — and moving the text engine's stack for the page
# reader's sake would have been the wrong side to bend.
#
# THE SET BELOW IS THE RESOLVE OF THE PREVIOUS SET PLUS `mlx-vlm==0.7.1`
# with every previous line held as a CONSTRAINT (`pip install -c <old>
# -r <old> mlx-vlm==0.7.1`, python 3.11.16, 2026-09-21), so nothing already
# here changed version and the 22 names that arrived are exactly mlx-vlm's
# closure: fastapi, starlette, uvicorn, python-multipart, pydantic and
# pydantic_core (its server, which Crucible does not run), llguidance,
# miniaudio, opencv-python, pillow, scipy, cffi, pycparser, annotated-types,
# typing-inspection, charset-normalizer, requests, urllib3, websockets, and
# `mlx-audio==0.5.5` with `sounddevice`, which mlx-vlm declares and this env
# has no use for (the tts env pins its own mlx-audio; that is a second copy
# at a different version in a second env, known and accepted).
#
# VALIDATED AS THIS EXACT SET: a venv built from it read four real 200-dpi
# book pages through the reader's own path byte-identically to
# `mlx_vlm.batch_generate` (the numbers are in models/dots-ocr.toml's
# [backends.mlx-darwin] block).
#
# Regenerate after any deliberate bump with:
#     ~/.crucible/envs/llm/bin/python -m pip freeze

mlx-lm==0.31.3
mlx-vlm==0.7.1

# --- the set pip resolved under them ---
annotated-doc==0.0.5
annotated-types==0.8.0
anyio==4.15.1
certifi==2026.7.22
cffi==2.1.1
charset-normalizer==3.5.1
click==8.5.0
fastapi==0.141.1
filelock==3.32.6
fsspec==2026.7.0
h11==0.16.0
hf-xet==1.6.0
httpcore==1.0.9
httpx==0.28.1
huggingface_hub==1.31.0
idna==3.19
Jinja2==3.1.6
llguidance==1.8.0
markdown-it-py==4.2.0
MarkupSafe==3.0.3
mdurl==0.1.2
miniaudio==1.71
mlx-audio==0.5.5
mlx-metal==0.32.2
mlx==0.32.2
numpy==2.4.6
opencv-python==5.0.0.93
packaging==26.3
pillow==12.3.0
protobuf==7.36.1
pycparser==3.0
pydantic==2.13.5
pydantic_core==2.46.5
Pygments==2.21.0
python-multipart==0.0.32
PyYAML==6.0.3
regex==2026.9.10
requests==2.34.2
rich==15.0.0
safetensors==0.8.0
scipy==1.17.1
sentencepiece==0.2.2
shellingham==1.5.4
sounddevice==0.5.6
starlette==1.6.0
tokenizers==0.23.2
tqdm==4.70.1
transformers==5.17.0
typer==0.27.2
typing-inspection==0.4.4
typing_extensions==4.16.0
urllib3==2.8.0
uvicorn==0.53.0
websockets==17.1
