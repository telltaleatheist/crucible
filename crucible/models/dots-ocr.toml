# dots.ocr — the page reader (PHASE3-VLM.md section 4).
#
# The first manifest in this repo whose `modalities` carries `image`, and the
# only model here a client sends a picture to. Foundry's page reading is an
# ORDINARY chat completion whose first content part is a data-URI PNG — one page
# per request, twelve in flight, `temperature: 0`, `max_tokens <= 8192` — so this
# model is served through the same `llm` proxy as everything else and there is no
# `vlm-pages` job type to go with the file name.
#
# ------------------------------------------------------------------- the repo
#
# `rednote-hilab/dots.ocr` is the name both apps send today and the name
# PHASE3-VLM.md was written with. It is no longer the repo's own name: as of
# 2026-09-13 the hub answers `GET /api/models/rednote-hilab/dots.ocr` with a
# **307 to `/api/models/dots-studio/dots.ocr`**, and `?author=rednote-hilab`
# lists nothing at all — the org was renamed, weights and history intact. Both
# names report the same `main` at the same commit, so this is a rename and not a
# fork or a mirror.
#
# The pin below is that commit, read from the hub on 2026-09-13:
#   GET /api/models/dots-studio/dots.ocr/refs
#     -> main = c0111ce6bc07803dbc267932ffef0ae3a51dc951
#   GET /api/models/rednote-hilab/dots.ocr/refs  (through the 307)
#     -> the same sha
# and `resolve/c0111ce6.../config.json` returns the config quoted below, so the
# revision is one the puller can actually fetch rather than one read off a page.
#
# `hf_repo` names the repo as it is named now. Pinning the redirect would work
# today and would be a fallback — a name that resolves because somebody else's
# server is still forwarding it, which is exactly the kind of dependency that
# fails quietly on the day it stops. The client-side name is a different
# question and stays where it is: Foundry sends `--vlm-endpoint-model dots-ocr`,
# Crucible's own id, and the proxy rewrites nothing (PHASE3-VLM.md section 5).
#
# -------------------------------------------------------------- what it is
#
# From `config.json` at the pinned sha: `DotsOCRForCausalLM`, `model_type`
# `dots_ocr`, custom modeling code in the repo (`modeling_dots_ocr.py`,
# `configuration_dots.py`) — hence `--trust-remote-code`. A 28-layer, 1536-hidden
# language model with 12 attention heads over 2 KV heads, and a 42-layer vision
# tower at patch 14 with `spatial_merge_size` 2. The weights are two bf16
# safetensors shards totalling 6_078_431_736 B (5.661 GiB), read from the tree
# API at the pinned sha — about 3.04 B parameters, which is the `params_b` below.
#
# Foundry rasterises at a pinned `VLM_DPI = 200`, so a 468x760 pt page becomes a
# 1300x2112 PNG: floor(1300/14) x floor(2112/14) = 13_800 patches, merged 2x2 to
# **3_450 image tokens**. That plus the layout-all prompt plus an 8192-token
# answer is what has to fit the context, and it is why the context is 32768 and
# not less.

[model]
id = "dots-ocr"
family = "dots"
params_b = 3
context_default = 32768

# What the WEIGHTS support, read from the hub on 2026-09-16:
#   rednote-hilab/dots.ocr @ main
#   config.json `max_position_embeddings` = 131072
# It is the wall behind context_default above and behind any backend
# block's override; both are checked against it at load. It is also what
# stops the card-derived ceiling running away — see
# docs/FITS-AND-THE-CARD.md section 6.3.
trained_context = 131072
# The point of the file. `image` here is what makes the loader refuse
# `--skip-mm-profiling` in the block below — the flag the two text manifests
# carry, worth a measured 1.90 GiB on the 9B, and the one thing that must NOT be
# set on a model that will actually be handed a page.
modalities = ["text", "image"]
# The two display facts (PHASE9-CAPABILITY.md, "The local form"), required by
# the [local] table below.
display = "dots.ocr"
description = "The page reader: a 3B vision-language model that turns a rasterised page into its text and layout. Two files on a machine without Crucible — the text tower and its vision projector."

# ------------------------------------------------------------ the local form
#
# What a machine with NO Crucible runs to read a page: llama-server with
# `-m <file> --mmproj <mmproj>` — Foundry's local reader (PHASE9-CAPABILITY.md,
# "The local form: one catalog, two doors"). TWO files, because llama.cpp
# serves a vision model as a text tower plus a projector, and a server started
# without the projector loads, answers /v1/models and refuses every page. The
# loader refuses a `[local]` GGUF on an `image` model that has no `mmproj` for
# exactly that reason.
#
# ##########################################################################
# THE PAIR MOVED ON 2026-09-15, AND A MEASUREMENT IS WHY.
#
# What this table used to name was `anthonym21/dots.ocr-GGUF` @
# `42ab310215a26d05ebe21ccc55f64db6c2bfc6ce` — `Dots.Ocr-1.8B-Q8_0.gguf` plus
# `mmproj-Dots.Ocr-F16.gguf`. T7 pulled exactly those two files onto the live
# Windows machine, `llama-server` started, and it exited before it served:
#
#   E clip_init: failed to load model '...mmproj-Dots.Ocr-F16.gguf':
#     Key not found: clip.vision.projector.scale_factor
#   E srv load_model: failed to load multimodal model
#   E srv llama_server: exiting due to model loading error
#   (C:\tmp\phase15-testrun\home\logs\engine-dots-ocr.log, 2026-09-15 00:27)
#
# THE KEY IS THE WHOLE FAULT, and both halves of it were read off the bytes
# rather than reasoned about:
#
#   * The pulled `mmproj-Dots.Ocr-F16.gguf` (GGUF v3, 388 tensors, 25 kv) has
#     NO `clip.vision.projector.scale_factor`. It carries
#     `clip.vision.spatial_merge_size = 2` instead — the OLD spelling, which
#     upstream llama.cpp reads for qwen2vl-family projectors and never for
#     this one.
#   * `tools/mtmd/clip.cpp` at the pinned `b10970`, case
#     `PROJECTOR_TYPE_DOTS_OCR` (line 1542):
#       get_u32(KEY_PROJ_SCALE_FACTOR, hparams.n_merge);
#     with NO `required = false` third argument, where every optional read in
#     that function passes one. `KEY_PROJ_SCALE_FACTOR` is
#     `"clip.vision.projector.scale_factor"` (`tools/mtmd/clip-impl.h:61`).
#     So for dots.ocr the key is MANDATORY, and a projector without it is
#     refused at `clip_init` before a single page is seen.
#
# THE PIN DOES NOT MOVE — THE PROJECTOR DOES. The same line is byte-identical
# at `b10950`, Foundry's own fallback build (`page-reader.ts`,
# `PINNED_RELEASE`), so this is not a regression between two builds and no
# choice of upstream tag makes that file load. The reason is in the repo's own
# README, which this manifest previously quoted only half of: after the
# 2026-03-23 regeneration note it says *"Requires a llama.cpp build with
# DotsOCR support. At the moment, use: anthony-maio/llama.cpp"* — a FORK. Those
# GGUFs were converted by a fork's converter for that fork's loader. Pinning
# them against `ggml-org/llama.cpp` was pinning two halves of two different
# projects.
#
# WHAT IS PINNED NOW is llama.cpp's own conversion, from the org that merged
# dots.ocr support (PR #17575, the PR `page-reader.ts` cites) — and it is ALSO
# the pair Foundry named before it started reading this catalog, so the two
# apps come back to ONE combination rather than a third:
#
#   foundry @ 06efb83, app/electron/page-reader.ts (the constants Wave 61
#   package E deleted when the catalog became the owner):
#     const HF_REPO     = 'ggml-org/dots.ocr-GGUF';
#     const MODEL_FILE  = 'dots.ocr-Q8_0.gguf';
#     const MMPROJ_FILE = 'mmproj-dots.ocr-Q8_0.gguf';
#
# Its projector carries the key. Read from the file's own header over a range
# request on 2026-09-15 (GGUF v3, 304 tensors, 24 kv):
#     clip.projector_type = dots_ocr
#     clip.vision.projector.scale_factor = 2
#     clip.vision.image_min_pixels = 3136
#     clip.vision.image_max_pixels = 11289600
# and `clip.vision.spatial_merge_size` is absent, which is the same fact from
# the other side: this is the conversion upstream's reader was written for.
#
# Every number, with where it came from and when (hub API, 2026-09-15, at the
# pinned sha):
#   hf_repo/revision  ggml-org/dots.ocr-GGUF @ 2c093a32ca360a396bc6d87d60408636130b9d9b
#                     GET /api/models/ggml-org/dots.ocr-GGUF/refs -> main = that
#                     sha (no tags, no other branches); the repo's lastModified
#                     is 2026-04-05, so this is a settled repository and not one
#                     being re-cut under the pin.
#   file              dots.ocr-Q8_0.gguf          1_894_530_272 B  (tree API, LFS size)
#                     sha256 6f2db1a75215893760b7c7185d52f11715d2e0cb7bff9cefd3f6d409fa1de7e9
#   mmproj            mmproj-dots.ocr-Q8_0.gguf   1_344_068_512 B  (tree API, LFS size)
#                     sha256 20d3d35bea3a64ead0b4b213f8db0de136e174bb5b75b8e85786c51e8e822423
#                     (the LFS oid IS the sha256; nothing in this schema pins a
#                     digest, so it is recorded here and the revision is what
#                     the puller enforces)
#   download_bytes    3_238_598_784 — the two, added. 1.18 GB LESS than the pair
#                     it replaces.
#   needs_bytes       4_738_598_784 — download + 1_500_000_000, DECLARED, the same
#                     discipline as the two Ollama rows (Foundry's `OVERHEAD_GB`).
#                     What the 1.5 GB has to cover here: KV at the 32768 context
#                     this manifest serves is 28_672 B/token x 32_768 = 0.94 GB
#                     (the arithmetic in the cuda-linux block below), and the
#                     vision tower's activations for one ~3_450-token page are
#                     the rest. Not watched on a card.
#
# WHY THE Q8_0 PROJECTOR AND NOT THE F16 ONE IN THE SAME REPO. The repo also
# holds `mmproj-dots.ocr-f16.gguf` (2_526_296_992 B, sha256
# f462429dbf41729379df4252f84331c9646d80e53f092ccccfd2cb922a3b544e), and its
# header carries `clip.vision.projector.scale_factor = 2` as well — it would
# load. The Q8_0 one is chosen because it is what Foundry shipped, which is
# the whole point of this change: ONE combination true in both apps, restored
# rather than invented. The general guidance this file used to cite — that an
# mmproj is small and quantising it costs more than it saves — was written
# about projectors of a few hundred megabytes, and dots.ocr's vision tower is
# 1.2 B parameters; here F16 costs 1.18 GB of download and 1.18 GB of card.
# If the Q8 projector turns out to read pages worse, THAT is the measurement
# that moves this line — `mmproj-dots.ocr-f16.gguf` at the same revision, and
# nothing else changes. It is not a fallback and nothing selects it at
# runtime; it is the next pin if a page says so.
# ##########################################################################
[local]
kind = "gguf"
hf_repo = "ggml-org/dots.ocr-GGUF"
revision = "2c093a32ca360a396bc6d87d60408636130b9d9b"
file = "dots.ocr-Q8_0.gguf"
mmproj = "mmproj-dots.ocr-Q8_0.gguf"
download_bytes = 3_238_598_784
needs_bytes = 4_738_598_784
needs_basis = "declared"

[backends.cuda-linux]
engine = "vllm"
hf_repo = "dots-studio/dots.ocr"
revision = "c0111ce6bc07803dbc267932ffef0ae3a51dc951"
# ##########################################################################
# DECLARED, NOT MEASURED. This is the one number in models/ that was not read
# off an accelerator, and it is labelled so nobody later mistakes it for one.
# The 3090 Ti was running an overnight fine-tune the night this manifest was
# written and the Mac was on an audio job, so the honest choice was between a
# number with its derivation shown and no manifest at all.
#
# It is NOT invented. Both apps that serve this model on this exact card
# converge on the same reservation, and the number below is that reservation:
#
#   BookForge, electron/vlm-page-server.ts — `RESERVE_CAP_MB = 12_288`, a cap
#   on the reservation rather than a fraction of the card, and the comment
#   there names the incident that set it: on 2026-08-11 a ~20 GiB reservation
#   (the fraction ceiling applied to an idle card) held the machine at 93%
#   commit all night and OOM-killed bun, ffmpeg and python around it, because
#   under WSL's dxg layer every reserved GiB is ALSO committed host RAM.
#
#   Foundry, app/electron/vllm-server.ts — `GPU_UTIL = 0.5`, flat, for the same
#   stated reason: "half is clear of the ~6 GB of weights plus a workable KV
#   cache ... reserving for the card's emptiness rather than the model's need is
#   a way to OOM the machine around it."
#
# 12_288 MiB of a 24_564 MiB card is 0.5003, which is where the 0.5 below comes
# from, and this is the configuration that reads Owen's books today.
#
# Why the estimate equals the budget: `--gpu-memory-utilization` is a fraction
# of the card's TOTAL and a BUDGET rather than a demand — vLLM sizes the KV pool
# to fill whatever the weights, the profiled activation peak and the CUDA graphs
# leave of it (models/qwen3.5-9b.toml explains this at length). So the engine's
# share is the budget, whatever the model is. Against the two blocks in this
# repo where both figures exist:
#   qwen3.5-9b       util 0.84 -> budget 20.15 GiB, MEASURED peak 19.52 GiB
#   qwen3.8-27b-4bit util 0.86 -> budget 20.63 GiB, MEASURED peak 20.15 GiB
# the budget over-states the measured peak by 0.6 and 0.5 GiB. Over-stating is
# the safe direction for a guard whose job is to refuse a load there is no room
# for, so the budget is what goes in:
#   0.5 x 24_564 MiB = 12_282 MiB = 12_878_610_432 B = 11.99 GiB
#
# The floor, for the same reason the 27B manifests carry theirs — weights plus
# KV at this context, COMPUTED from config.json at the pinned sha:
#   head_dim = hidden_size / num_attention_heads = 1536 / 12 = 128
#   per token per layer = 2 (K and V) x 2 kv heads x 128 x 2 bytes = 1_024 B
#   per token = 1_024 x 28 layers = 28_672 B
#   at 32_768 tokens = 939_524_096 B (0.875 GiB)
#   6_078_431_736 (weights) + 939_524_096 (KV) = 7_017_955_832 B = 6.54 GiB
# so the declared figure leaves about 5.4 GiB for the vision tower's activation
# peak, the CUDA graphs and the KV of more than one page at a time. That last
# one is the part to watch: a page in flight is ~3_450 image tokens plus the
# prompt plus up to 8192 of answer, so at the worst case 12 concurrent pages
# want about 4.1 GiB of KV. If the pool comes out under that vLLM PREEMPTS and
# the run slows down; it does not fail, and it does not OOM the host.
#
# WHAT MUST BE MEASURED, AND WHEN: the first time a card is free. Pull this
# model, load it, read one real page through the proxy, and sample nvidia-smi
# across the run the way scripts/measure-llm-memory.sh does for the text
# models — then replace this number and this comment with the measured peak and
# the run that produced it. In particular the profiled activation peak WITH an
# image in it is a different number from anything the text models measured,
# because `--skip-mm-profiling` is precisely what they used to avoid measuring
# it. Until that run happens, treat 0.5 as a working configuration and not as a
# tuned one.
# ##########################################################################
memory_bytes_estimate = 12_878_610_432
# `--trust-remote-code`: dots.ocr ships its modeling class in its own repo
# (`auto_map` points at `modeling_dots_ocr.DotsOCRForCausalLM`), and vLLM refuses
# the repo without this — by name, in a pydantic ValidationError, which is how
# BookForge found it rather than a user.
#
# `--max-model-len 32768`: the model's `max_position_embeddings` is 131072, so
# this is a choice about the card and not a limit of the weights. It is what both
# apps serve, and PHASE3-VLM.md's floor: 3_450 image tokens plus the layout-all
# prompt plus an 8192-token answer do not fit less with room to spare.
#
# `--max-num-seqs 16`: Foundry's ungated default is twelve pages in flight and
# BookForge takes it by passing `concurrency: 0`, so twelve is the floor a page
# reader has to clear. Sixteen is the number the other two manifests use and the
# number BookForge's own text server runs on this card. Neither app passes this
# flag at all today, which leaves vLLM's default in place; it is set here for the
# 9B's measured reason — the count of CUDA graphs vLLM captures follows it, and
# at the default the 9B captured 51 of them and paged at 87 s for one.
#
# No `--dtype`: the checkpoint's config already says `bfloat16`, and neither
# working launch line overrides it, so there is nothing here to override.
#
# No `--skip-mm-profiling`, and the loader will not accept one beside
# `modalities = [..., "image"]` (PHASE3-VLM.md section 3). This is the model that
# flag was always going to be wrong for.
engine_args = [
    "--trust-remote-code",
    "--max-model-len", "32768",
    "--gpu-memory-utilization", "0.5",
    "--max-num-seqs", "16",
]
# NO `max_context`: the estimate here is a budget (0.5 x the card), not a sum of
# terms, so there is nothing to compute a maximum from (below). The ceiling is
# this block's 32768.

# ---------------------------------------- NO [memory] TABLE HERE, DELIBERATELY
#
# `memory_bytes_estimate` above is not a sum of terms at all -- it is the BUDGET,
# 0.5 x 24_564 MiB, and the comment says so: "the engine's share is the budget,
# whatever the model is". Taking a budget apart into weights, overhead and a
# slope would produce three numbers that add to the right total and describe
# nothing, which is worse than no table.
#
# What the comment DOES give -- 6_078_431_736 B of weights and 28_672 B/token
# COMPUTED -- leaves about 5.4 GiB unaccounted, and that residual is explicitly
# two different things at once: the vision tower's activation peak and the CUDA
# graphs, which are fixed, AND "the KV of more than one page at a time", which is
# the concurrency term. Putting the whole of it in `overhead_bytes` would count
# the second part again the moment anything asked what four pages cost.
#
# The measurement this wants is already written down two screens up ("WHAT MUST
# BE MEASURED, AND WHEN: the first time a card is free"), and calibration at two
# contexts answers it in the same run.

# ###########################################################################
# `[backends.mlx-darwin]` — WRITTEN 2026-09-21, THE WAY 7c SAID IT COULD BE.
#
# The finding that kept this block out for a week stands and is recorded in
# `crucible/engines/mlx_vlm.py`: mlx-vlm's OWN HTTP server never expands the
# image into the prompt for dots.ocr (`prompt_tokens=216`, one `Picture` box).
# What changed is that Crucible no longer drives that server. The engine is
# `crucible/engines/mlx_vlm_serve.py`, Crucible's own process running the
# in-process path — the one that was always correct — behind the two routes
# the proxy speaks. Everything below was measured on Owen's Mac Studio
# (M1 Ultra, 64 GiB) on 2026-09-21 against these weights, in a venv built from
# `envs/llm/mlx-darwin.txt` exactly as it now reads.
#
# THE WEIGHTS. `mlx-community/dots.ocr-bf16` — bf16, not the 4-bit the earlier
# run used: Owen's call, to match the PC's precision (the 4-bit is text-lossless
# on every hard page tried; bf16 is parity, not a quality win). Read from the
# hub on 2026-09-21: `/refs` -> main = the sha below; `/tree/main?recursive=1`
# -> 19 files, 6_092_753_640 B, of which the two safetensors shards are
# 5_341_909_500 + 736_527_188. No `--trust-remote-code`: mlx-vlm ships its own
# `dots_ocr` model class and the repo's `auto_map` is never followed.
#
# THE WIDTH, `--width 12`, from a sweep over sixteen real 739x1259 pages
# (this book's mass-market size at 200 dpi; 1,385 prompt tokens each), text
# and category IDENTICAL to serial at every width:
#     width   1      2      4      8     12     16
#     s/page  9.6   12.5    7.9    5.7    5.5    4.2
#     peak    7.65   7.65   8.15   9.03  10.00  12.41  GB
# Twelve, because that is `crucible/pages.py`'s PAGE_CONCURRENCY — the most
# a client ever has in flight, so a wider batch can never fill. (Width 2 is
# slower than serial, reproducibly, on 4 and on 16 pages. Not understood;
# recorded so nobody "optimises" down to it.)
#
# THE PAGE SIZE THAT MATTERS is not this book's. A 468x760 pt paperback at the
# pinned 200 dpi is 1300x2112 px, ~3,900 prompt tokens, and twelve of those
# through the vision tower in one call is what the Metal watchdog kills
# (`Impacting Interactivity`). The engine embeds one image at a time for that
# reason, and the number below comes from THAT case: twelve 1300x2232 pages
# at width 12, all twelve identical to serial:
#     serial   20.22 s/page   peak  8_268_786_409 B
#     width 12 14.45 s/page   peak 13_963_861_086 B
# and a smaller language-model prefill batch is NOT a lever — prefill 1 read
# one page correctly and prefill 4 read four; the rest were garbage or
# 8192-token runaways. The engine keeps prefill == batch, upstream's invariant.
#
# `memory_bytes_estimate` IS THAT MEASURED PEAK — `mx.get_peak_memory()` over
# the twelve-page width-12 run on the large pages, reset before the load. It
# is the largest figure any run produced, and it is what the machine must be
# able to give while a full batch of paperback pages is in flight. MEASURED,
# not declared, and the first block in this file that can say so.
#
# `context_default` is what this server REPORTS, as on every mlx block: the
# engine takes no context flag and mlx-vlm uses the model's own config; 32768
# is what a client sizes against and matches cuda-linux.
[backends.mlx-darwin]
engine = "mlx-vlm"
hf_repo = "mlx-community/dots.ocr-bf16"
revision = "98b9cda3930eb69d2b887781685f3f393d6f141e"
memory_bytes_estimate = 13_963_861_086
context_default = 32768
engine_args = ["--width", "12"]
# NO `max_context`: this block has no [memory] terms to compute one from, and
# the `pages` class's context is its own ruling (32768), not a client's.

# ---------------------------------------- NO [memory] TABLE HERE, DELIBERATELY
#
# The estimate above is one measured peak at one width on one page size, and
# the two terms a table wants — what the weights hold and what each token of
# KV adds — were not taken apart in that run. Splitting a measured total into
# terms nobody measured would be the cuda-linux block's own objection, written
# on the other backend. A calibration run at two widths is where the terms
# come from, if a class ever needs to ask this model a different question.
# ###########################################################################

# ---------------------------------------------------------------------------
# PHASE15-HOST.md sections 0 and 3.10. Windows natively, llama.cpp on the SAME
# two GGUF files the `[local]` table above already names — one repo, one
# revision, one pair. That is not a coincidence and it is the point: `[local]`
# describes the form of this model on a machine with no Crucible, and
# `llama-windows` is Crucible USING that form, so a second pin here would be
# ARCHITECTURE.md R1's defect written on purpose in one file.
#
# `memory_bytes_estimate` is `[local] needs_bytes` — 3.24 GB of weights plus
# Foundry's own 1.5 GB OVERHEAD_GB for KV at `-c 16384` and the runner's
# buffers. DECLARED, not measured: no Windows box has served a page yet, and
# section 3.10's fact 8 says so out loud — seconds per page and whether the Q8
# answers in the parser's dialect are both UNMEASURED and are the first two
# things measured on a card. T7 got as far as the spawn on 2026-09-15 and the
# projector refused to load (the block above), which is a measurement of the
# PIN and not of the model.
#
# `-c 16384` and `--parallel 1` are Foundry's working launcher verbatim
# (`ensurePageReader()`): a page at the app's 200 dpi is up to ~8k image
# tokens plus the answer, and one page at a time is what the guard and the
# lease already assume. The `-m` and `--mmproj` paths are NOT here — they are
# composed by `Residency._engine_args` from `file` and `mmproj` below, because
# only the server knows where it put the weights.
[backends.llama-windows]
engine = "llama-server"
hf_repo = "ggml-org/dots.ocr-GGUF"
revision = "2c093a32ca360a396bc6d87d60408636130b9d9b"
file = "dots.ocr-Q8_0.gguf"
mmproj = "mmproj-dots.ocr-Q8_0.gguf"
memory_bytes_estimate = 4_738_598_784
context_default = 16384
engine_args = ["--parallel", "1"]
# NO `max_context`: no [memory] terms here to compute one from; the ceiling is
# this block's own 16384.

# ---------------------------------------- NO [memory] TABLE HERE, DELIBERATELY
#
# The estimate is 3.24 GB of weights plus Foundry's flat 1.5 GB, and splitting
# that 1.5 GB the way the two text manifests split theirs needs this model's KV
# rate per token. The 9B and the 27B both state theirs, computed from
# config.json, in the blocks that use them; dots.ocr's is stated nowhere in this
# file, and a rate worked out here would be the one number in this table nothing
# cites. The first Windows load -- which section 3.10 fact 8 already owes -- is
# where it comes from.
